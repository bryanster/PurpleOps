dist
coverage
node_modules
package-lock.json

# Generated by openapi-typescript from api/openapi.yaml. `make generate` must be
# a no-op on a clean tree, and it would not be if Prettier rewrote the output.
src/api/schema.d.ts

# Vendored from the shadcn/ui registry and reformatted only when upstream does.
# Running Prettier over them would rewrite every file on the first `add` and
# make the next upstream diff unreadable.
src/components/ui
