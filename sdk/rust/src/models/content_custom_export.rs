/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ContentCustomExport : Export document for custom content. Shape is accepted by the v1/custom import path (M2-012) or documented as the delta if the importer needs a thin adapter. Empty arrays mean that family was omitted or empty. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ContentCustomExport {
    #[serde(rename = "meta")]
    pub meta: models::ContentCustomExportMeta,
    #[serde(rename = "procedureTemplates")]
    pub procedure_templates: Vec<models::ContentProcedureTemplate>,
    #[serde(rename = "detectionRules")]
    pub detection_rules: Vec<models::ContentDetectionRule>,
    #[serde(rename = "notes")]
    pub notes: Vec<models::ContentNote>,
}

impl ContentCustomExport {
    /// Export document for custom content. Shape is accepted by the v1/custom import path (M2-012) or documented as the delta if the importer needs a thin adapter. Empty arrays mean that family was omitted or empty. 
    pub fn new(meta: models::ContentCustomExportMeta, procedure_templates: Vec<models::ContentProcedureTemplate>, detection_rules: Vec<models::ContentDetectionRule>, notes: Vec<models::ContentNote>) -> ContentCustomExport {
        ContentCustomExport {
            meta,
            procedure_templates,
            detection_rules,
            notes,
        }
    }
}

