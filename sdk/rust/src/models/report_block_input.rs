/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ReportBlockInput {
    #[serde(rename = "blockId")]
    pub block_id: String,
    /// Block parameters, validated against the registry's ParamSchema. Defaults are used from the registry when absent. HTML content within params (e.g. rich_text `html`) is limited to 100 KiB raw and sanitized server-side on write (M6-005).  Parameter names come from the block's own ParamSchema and are not interchangeable: a key the schema does not declare is rejected, so a client that misspells one fails the whole save. 
    #[serde(rename = "params", skip_serializing_if = "Option::is_none")]
    pub params: Option<serde_json::Value>,
}

impl ReportBlockInput {
    pub fn new(block_id: String) -> ReportBlockInput {
        ReportBlockInput {
            block_id,
            params: None,
        }
    }
}

