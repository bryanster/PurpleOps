/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// RevokedSessions : What `POST /users/{userId}/sessions/revoke` or `POST /auth/sessions/revoke-others` ended. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct RevokedSessions {
    /// How many live sessions were ended. Zero is a normal answer: the account may have had none, or somebody may have revoked them a moment ago. 
    #[serde(rename = "revoked")]
    pub revoked: i32,
}

impl RevokedSessions {
    /// What `POST /users/{userId}/sessions/revoke` or `POST /auth/sessions/revoke-others` ended. 
    pub fn new(revoked: i32) -> RevokedSessions {
        RevokedSessions {
            revoked,
        }
    }
}

