/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ActivityPage : One page of the activity log. `nextCursor` is taken verbatim from a previous response; absent (or null) means there is no further page. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ActivityPage {
    #[serde(rename = "items")]
    pub items: Vec<models::ActivityEntry>,
    /// Opaque cursor for the next page. Pass it back as `cursor`. Clients must not parse or construct one. 
    #[serde(rename = "nextCursor", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub next_cursor: Option<Option<String>>,
}

impl ActivityPage {
    /// One page of the activity log. `nextCursor` is taken verbatim from a previous response; absent (or null) means there is no further page. 
    pub fn new(items: Vec<models::ActivityEntry>) -> ActivityPage {
        ActivityPage {
            items,
            next_cursor: None,
        }
    }
}

