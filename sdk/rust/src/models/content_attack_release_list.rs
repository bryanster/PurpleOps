/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ContentAttackReleaseList {
    #[serde(rename = "items")]
    pub items: Vec<models::ContentAttackRelease>,
    /// Whether the upstream index answered. `false` is a normal outcome on an air-gapped installation, not a failed request. 
    #[serde(rename = "reachable")]
    pub reachable: bool,
    /// Why the index could not be read, when `reachable` is false — the transport or parse error, for an administrator reading it beside the source URL they configured. Null otherwise. 
    #[serde(rename = "unreachable", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub unreachable: Option<Option<String>>,
    /// Whether the ATT&CK source is enabled. A sync started against a disabled source is refused, so a picker should say so before offering one. 
    #[serde(rename = "sourceEnabled")]
    pub source_enabled: bool,
}

impl ContentAttackReleaseList {
    pub fn new(items: Vec<models::ContentAttackRelease>, reachable: bool, source_enabled: bool) -> ContentAttackReleaseList {
        ContentAttackReleaseList {
            items,
            reachable,
            unreachable: None,
            source_enabled,
        }
    }
}

