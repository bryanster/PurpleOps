/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ImportPlanResult {
    #[serde(rename = "scenario")]
    pub scenario: models::Scenario,
    #[serde(rename = "steps")]
    pub steps: Vec<models::Step>,
    /// Total number of steps imported.
    #[serde(rename = "stepCount")]
    pub step_count: i32,
    /// Steps whose technique external id did not resolve in the engagement's pinned ATT&CK version. These steps were still imported, but their technique/tactic/subtechnique fields are empty. 
    #[serde(rename = "warnings")]
    pub warnings: Vec<models::ImportPlanWarning>,
}

impl ImportPlanResult {
    pub fn new(scenario: models::Scenario, steps: Vec<models::Step>, step_count: i32, warnings: Vec<models::ImportPlanWarning>) -> ImportPlanResult {
        ImportPlanResult {
            scenario,
            steps,
            step_count,
            warnings,
        }
    }
}

