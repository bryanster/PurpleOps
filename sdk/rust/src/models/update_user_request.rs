/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// UpdateUserRequest : Body of `PATCH /users/{userId}`. Every field is optional and an absent one is left alone, so two administrators editing different things do not overwrite each other. At least one must be present — an empty patch is a client bug, and answering it `200` would hide one.  `email` is deliberately not a field: it is what a federated sign-in links an account by, so editing it could move somebody else's single sign-on onto this account. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct UpdateUserRequest {
    #[serde(rename = "displayName", skip_serializing_if = "Option::is_none")]
    pub display_name: Option<String>,
    #[serde(rename = "platformRole", skip_serializing_if = "Option::is_none")]
    pub platform_role: Option<models::PlatformRole>,
    #[serde(rename = "status", skip_serializing_if = "Option::is_none")]
    pub status: Option<models::UserStatus>,
    #[serde(rename = "mfaEnforced", skip_serializing_if = "Option::is_none")]
    pub mfa_enforced: Option<bool>,
}

impl UpdateUserRequest {
    /// Body of `PATCH /users/{userId}`. Every field is optional and an absent one is left alone, so two administrators editing different things do not overwrite each other. At least one must be present — an empty patch is a client bug, and answering it `200` would hide one.  `email` is deliberately not a field: it is what a federated sign-in links an account by, so editing it could move somebody else's single sign-on onto this account. 
    pub fn new() -> UpdateUserRequest {
        UpdateUserRequest {
            display_name: None,
            platform_role: None,
            status: None,
            mfa_enforced: None,
        }
    }
}

