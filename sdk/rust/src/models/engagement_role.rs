/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// EngagementRole : What somebody may do inside one engagement. Red and blue are separate so that blind mode and the split write endpoints have something to decide on. 
/// What somebody may do inside one engagement. Red and blue are separate so that blind mode and the split write endpoints have something to decide on. 
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum EngagementRole {
    #[serde(rename = "lead")]
    Lead,
    #[serde(rename = "red")]
    Red,
    #[serde(rename = "blue")]
    Blue,
    #[serde(rename = "observer")]
    Observer,

}

impl std::fmt::Display for EngagementRole {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        match self {
            Self::Lead => write!(f, "lead"),
            Self::Red => write!(f, "red"),
            Self::Blue => write!(f, "blue"),
            Self::Observer => write!(f, "observer"),
        }
    }
}

impl Default for EngagementRole {
    fn default() -> EngagementRole {
        Self::Lead
    }
}

