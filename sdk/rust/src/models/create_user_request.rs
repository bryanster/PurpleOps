/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// CreateUserRequest : Body of `POST /users`. The identifier, the timestamps and the invite link are the server's; everything a caller chooses is here. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct CreateUserRequest {
    /// The address, stored as typed and compared without regard to case or surrounding whitespace. An address another account already holds is `409`. 
    #[serde(rename = "email")]
    pub email: String,
    #[serde(rename = "displayName")]
    pub display_name: String,
    #[serde(rename = "platformRole")]
    pub platform_role: models::PlatformRole,
    /// A password for a local account. Omit it for an account that signs in through the identity provider.  The bounds here are a backstop, not the policy: the policy lives in one place (internal/authn/password) and is reported as field errors on `password`, so there is one definition of an acceptable password rather than one here and one in the client. 
    #[serde(rename = "password", skip_serializing_if = "Option::is_none")]
    pub password: Option<String>,
    /// The state to create the account in. Omitted, it is derived from `password`: `active` with one, `invited` without.  `disabled` is not offered — creating an account that cannot be used is a way of describing an account that should not have been created. An `invited` account with a password is refused with a field error: an invited account has no local sign-in, so the password would be one nobody could ever use. 
    #[serde(rename = "status", skip_serializing_if = "Option::is_none")]
    pub status: Option<Status>,
    /// Require a second factor of this person specifically. Defaults to false.
    #[serde(rename = "mfaEnforced", skip_serializing_if = "Option::is_none")]
    pub mfa_enforced: Option<bool>,
}

impl CreateUserRequest {
    /// Body of `POST /users`. The identifier, the timestamps and the invite link are the server's; everything a caller chooses is here. 
    pub fn new(email: String, display_name: String, platform_role: models::PlatformRole) -> CreateUserRequest {
        CreateUserRequest {
            email,
            display_name,
            platform_role,
            password: None,
            status: None,
            mfa_enforced: None,
        }
    }
}
/// The state to create the account in. Omitted, it is derived from `password`: `active` with one, `invited` without.  `disabled` is not offered — creating an account that cannot be used is a way of describing an account that should not have been created. An `invited` account with a password is refused with a field error: an invited account has no local sign-in, so the password would be one nobody could ever use. 
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Status {
    #[serde(rename = "invited")]
    Invited,
    #[serde(rename = "active")]
    Active,
}

impl Default for Status {
    fn default() -> Status {
        Self::Invited
    }
}

