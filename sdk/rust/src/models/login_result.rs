/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// LoginResult : The outcome of a successful `POST /auth/login`.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct LoginResult {
    #[serde(rename = "status")]
    pub status: models::LoginStatus,
    /// The signed-in user. Present when `status` is `authenticated` or `mfa_enrolment_required` — both of those issued a session, and the second one needs `mfa` to render the enrolment screen it is confined to.  Absent when `status` is `mfa_required`: a caller awaiting a second factor has no session and has not been told who they are yet. 
    #[serde(rename = "user", skip_serializing_if = "Option::is_none")]
    pub user: Option<models::CurrentUser>,
}

impl LoginResult {
    /// The outcome of a successful `POST /auth/login`.
    pub fn new(status: models::LoginStatus) -> LoginResult {
        LoginResult {
            status,
            user: None,
        }
    }
}

