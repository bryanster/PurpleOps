/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// SetupState : Whether this installation has been through the first-run wizard.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct SetupState {
    /// Whether somebody finished the wizard. A decision, not an outcome: it does not mean content is installed. 
    #[serde(rename = "completed")]
    pub completed: bool,
    /// When it was finished. Absent while `completed` is false.
    #[serde(rename = "completedAt", skip_serializing_if = "Option::is_none")]
    pub completed_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    /// The user who finished it, or null when nothing did — `blctl setup complete` on a provisioning run has no person to attribute. 
    #[serde(rename = "completedBy", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub completed_by: Option<Option<String>>,
}

impl SetupState {
    /// Whether this installation has been through the first-run wizard.
    pub fn new(completed: bool) -> SetupState {
        SetupState {
            completed,
            completed_at: None,
            completed_by: None,
        }
    }
}

