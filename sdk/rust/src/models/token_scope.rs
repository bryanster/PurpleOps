/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// TokenScope : One coarse permission a service token may carry (M1-011). Scopes are few and blunt deliberately: a scope per endpoint would be a second permission model to keep in step with the real one, and the real one is `docs/authz.md`.  A scope is never a grant on its own. Holding `admin:write` permits nothing the token's owner could not already do — see `POST /auth/tokens`. These words are the ones `internal/authz` requires of each action, and `TestTheTokenScopeEnumMatchesTheOnePlaceScopesAreDefined` fails the build if this list and that one disagree. 
/// One coarse permission a service token may carry (M1-011). Scopes are few and blunt deliberately: a scope per endpoint would be a second permission model to keep in step with the real one, and the real one is `docs/authz.md`.  A scope is never a grant on its own. Holding `admin:write` permits nothing the token's owner could not already do — see `POST /auth/tokens`. These words are the ones `internal/authz` requires of each action, and `TestTheTokenScopeEnumMatchesTheOnePlaceScopesAreDefined` fails the build if this list and that one disagree. 
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum TokenScope {
    #[serde(rename = "admin:read")]
    AdminColonRead,
    #[serde(rename = "admin:write")]
    AdminColonWrite,
    #[serde(rename = "content:read")]
    ContentColonRead,
    #[serde(rename = "content:sync")]
    ContentColonSync,
    #[serde(rename = "content:write")]
    ContentColonWrite,
    #[serde(rename = "engagements:read")]
    EngagementsColonRead,
    #[serde(rename = "engagements:write")]
    EngagementsColonWrite,
    #[serde(rename = "reports:read")]
    ReportsColonRead,
    #[serde(rename = "reports:write")]
    ReportsColonWrite,

}

impl std::fmt::Display for TokenScope {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        match self {
            Self::AdminColonRead => write!(f, "admin:read"),
            Self::AdminColonWrite => write!(f, "admin:write"),
            Self::ContentColonRead => write!(f, "content:read"),
            Self::ContentColonSync => write!(f, "content:sync"),
            Self::ContentColonWrite => write!(f, "content:write"),
            Self::EngagementsColonRead => write!(f, "engagements:read"),
            Self::EngagementsColonWrite => write!(f, "engagements:write"),
            Self::ReportsColonRead => write!(f, "reports:read"),
            Self::ReportsColonWrite => write!(f, "reports:write"),
        }
    }
}

impl Default for TokenScope {
    fn default() -> TokenScope {
        Self::AdminColonRead
    }
}

