/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PatchStep : Every field is optional; only the ones present are changed. Soft freeze prevents changes to identity fields after the step's execution leaves pending.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PatchStep {
    #[serde(rename = "name", skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    #[serde(rename = "objective", skip_serializing_if = "Option::is_none")]
    pub objective: Option<String>,
    #[serde(rename = "targetAsset", skip_serializing_if = "Option::is_none")]
    pub target_asset: Option<String>,
    #[serde(rename = "tools", skip_serializing_if = "Option::is_none")]
    pub tools: Option<Vec<String>>,
    #[serde(rename = "controlsInScope", skip_serializing_if = "Option::is_none")]
    pub controls_in_scope: Option<Vec<String>>,
}

impl PatchStep {
    /// Every field is optional; only the ones present are changed. Soft freeze prevents changes to identity fields after the step's execution leaves pending.
    pub fn new() -> PatchStep {
        PatchStep {
            name: None,
            objective: None,
            target_asset: None,
            tools: None,
            controls_in_scope: None,
        }
    }
}

