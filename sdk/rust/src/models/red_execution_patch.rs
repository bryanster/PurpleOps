/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// RedExecutionPatch : Red-side only PATCH body for an execution. `version` is the optimistic-lock field and is required on every call. Detection fields are not present — blue writes through a separate endpoint (M3-007) with its own type. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct RedExecutionPatch {
    /// The version the caller read. Mismatch → 409.
    #[serde(rename = "version")]
    pub version: i32,
    #[serde(rename = "status", skip_serializing_if = "Option::is_none")]
    pub status: Option<models::ExecutionStatus>,
    /// When execution began. If omitted on a →running transition, the server sets it to UTC now.
    #[serde(rename = "startedAt", skip_serializing_if = "Option::is_none")]
    pub started_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    #[serde(rename = "endedAt", skip_serializing_if = "Option::is_none")]
    pub ended_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    #[serde(rename = "commandRun", skip_serializing_if = "Option::is_none")]
    pub command_run: Option<String>,
    #[serde(rename = "sourceHost", skip_serializing_if = "Option::is_none")]
    pub source_host: Option<String>,
    #[serde(rename = "targetHost", skip_serializing_if = "Option::is_none")]
    pub target_host: Option<String>,
    #[serde(rename = "redNotes", skip_serializing_if = "Option::is_none")]
    pub red_notes: Option<String>,
}

impl RedExecutionPatch {
    /// Red-side only PATCH body for an execution. `version` is the optimistic-lock field and is required on every call. Detection fields are not present — blue writes through a separate endpoint (M3-007) with its own type. 
    pub fn new(version: i32) -> RedExecutionPatch {
        RedExecutionPatch {
            version,
            status: None,
            started_at: None,
            ended_at: None,
            command_run: None,
            source_host: None,
            target_host: None,
            red_notes: None,
        }
    }
}

