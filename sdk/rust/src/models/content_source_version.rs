/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ContentSourceVersion : One version snapshot under a source.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ContentSourceVersion {
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    #[serde(rename = "sourceId")]
    pub source_id: uuid::Uuid,
    /// Release label for ATT&CK (e.g. `15.1`), or the rolling token `current` for Atomic / Sigma / CTID / custom. 
    #[serde(rename = "version")]
    pub version: String,
    #[serde(rename = "status")]
    pub status: models::ContentSourceVersionStatus,
    #[serde(rename = "itemCount")]
    pub item_count: i64,
    /// When this version last finished successfully. Absent if never.
    #[serde(rename = "syncedAt", skip_serializing_if = "Option::is_none")]
    pub synced_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    #[serde(rename = "error")]
    pub error: String,
    /// Hex SHA-256 of the last successful raw snapshot. Empty if none.
    #[serde(rename = "rawSha256")]
    pub raw_sha256: String,
    /// Path of the raw snapshot relative to the content data root.
    #[serde(rename = "rawPath")]
    pub raw_path: String,
    #[serde(rename = "rawBytes")]
    pub raw_bytes: i64,
    #[serde(rename = "createdAt")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "updatedAt")]
    pub updated_at: chrono::DateTime<chrono::FixedOffset>,
}

impl ContentSourceVersion {
    /// One version snapshot under a source.
    pub fn new(id: uuid::Uuid, source_id: uuid::Uuid, version: String, status: models::ContentSourceVersionStatus, item_count: i64, error: String, raw_sha256: String, raw_path: String, raw_bytes: i64, created_at: chrono::DateTime<chrono::FixedOffset>, updated_at: chrono::DateTime<chrono::FixedOffset>) -> ContentSourceVersion {
        ContentSourceVersion {
            id,
            source_id,
            version,
            status,
            item_count,
            synced_at: None,
            error,
            raw_sha256,
            raw_path,
            raw_bytes,
            created_at,
            updated_at,
        }
    }
}

