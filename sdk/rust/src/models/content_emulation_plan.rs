/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ContentEmulationPlan : One CTID adversary emulation plan (catalog only in M2). Threat actor / group identity is `adversaryName`. Source metadata (attack_version, format_version, archive path) is in `metadata`. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ContentEmulationPlan {
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    #[serde(rename = "sourceId")]
    pub source_id: uuid::Uuid,
    /// Version token. CTID is rolling-head and always `current`.
    #[serde(rename = "version")]
    pub version: String,
    /// Stable id within the source. Prefer upstream `emulation_plan_details.id`; otherwise the actor directory slug (documented in `docs/content-ctid.md`). 
    #[serde(rename = "externalId")]
    pub external_id: String,
    /// Display name (usually the adversary name).
    #[serde(rename = "name")]
    pub name: String,
    #[serde(rename = "description")]
    pub description: String,
    /// Upstream threat-actor / group label (for example `FIN6`, `APT29`). Group refs are text labels in M2 — not resolved ATT&CK group rows. 
    #[serde(rename = "adversaryName")]
    pub adversary_name: String,
    /// Source-side bookkeeping: `attack_version`, `format_version`, archive `path`, `actor_slug` when known. 
    #[serde(rename = "metadata")]
    pub metadata: std::collections::HashMap<String, serde_json::Value>,
    #[serde(rename = "createdAt")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "updatedAt")]
    pub updated_at: chrono::DateTime<chrono::FixedOffset>,
}

impl ContentEmulationPlan {
    /// One CTID adversary emulation plan (catalog only in M2). Threat actor / group identity is `adversaryName`. Source metadata (attack_version, format_version, archive path) is in `metadata`. 
    pub fn new(id: uuid::Uuid, source_id: uuid::Uuid, version: String, external_id: String, name: String, description: String, adversary_name: String, metadata: std::collections::HashMap<String, serde_json::Value>, created_at: chrono::DateTime<chrono::FixedOffset>, updated_at: chrono::DateTime<chrono::FixedOffset>) -> ContentEmulationPlan {
        ContentEmulationPlan {
            id,
            source_id,
            version,
            external_id,
            name,
            description,
            adversary_name,
            metadata,
            created_at,
            updated_at,
        }
    }
}

