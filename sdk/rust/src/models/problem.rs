/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// Problem : RFC 9457 problem detail — the only error shape this API produces, served as `application/problem+json` (M0B-007).  Clients switch on `code`. `title` and `detail` are prose for a human and may be reworded at any time. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct Problem {
    /// URI reference identifying the problem type. `about:blank` when the status code says everything there is to say. 
    #[serde(rename = "type")]
    pub r#type: String,
    /// Short human-readable summary of the problem type. Stable for a given `type`.
    #[serde(rename = "title")]
    pub title: String,
    /// The HTTP status code, repeated so a logged or forwarded body stands alone.
    #[serde(rename = "status")]
    pub status: i32,
    /// Explanation specific to this occurrence. Never carries internal detail: an unrecognised server-side error is reported generically and the real error goes to the log. 
    #[serde(rename = "detail", skip_serializing_if = "Option::is_none")]
    pub detail: Option<String>,
    /// The request ID, echoed as `X-Request-Id`. A user quotes it in a bug report and an operator greps for it.  RFC 9457 types this as a URI reference; a bare request ID is a relative reference, and it is far more useful to a support desk than a URI that resolves to nothing. 
    #[serde(rename = "instance", skip_serializing_if = "Option::is_none")]
    pub instance: Option<String>,
    #[serde(rename = "code")]
    pub code: models::ProblemCode,
    /// Field-level failures. Present when `code` is `validation_failed`.
    #[serde(rename = "errors", skip_serializing_if = "Option::is_none")]
    pub errors: Option<Vec<models::FieldError>>,
}

impl Problem {
    /// RFC 9457 problem detail — the only error shape this API produces, served as `application/problem+json` (M0B-007).  Clients switch on `code`. `title` and `detail` are prose for a human and may be reworded at any time. 
    pub fn new(r#type: String, title: String, status: i32, code: models::ProblemCode) -> Problem {
        Problem {
            r#type,
            title,
            status,
            detail: None,
            instance: None,
            code,
            errors: None,
        }
    }
}

