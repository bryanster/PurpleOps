/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ServiceToken : One service token, as anybody but its creator ever sees it: everything about the credential except the credential. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ServiceToken {
    /// The token's identifier, and what `DELETE /auth/tokens/{tokenId}` names. Not a credential.
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    /// What its owner called it, so they can tell which one to revoke.
    #[serde(rename = "name")]
    pub name: String,
    /// The public identifier in the middle of the token — the `xxx` of `bl_xxx_…`. It is how a token found in a log or a CI variable is matched to a row here without anybody handling its secret. 
    #[serde(rename = "prefix")]
    pub prefix: String,
    /// What this token may do, subject to what its owner may do.
    #[serde(rename = "scopes")]
    pub scopes: Vec<models::TokenScope>,
    /// The one engagement this token may touch. Absent on a token that may reach every engagement its owner can. It only ever subtracts. 
    #[serde(rename = "engagementId", skip_serializing_if = "Option::is_none")]
    pub engagement_id: Option<uuid::Uuid>,
    #[serde(rename = "status")]
    pub status: models::ServiceTokenStatus,
    #[serde(rename = "createdAt")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    /// When it stops working. Always set — a token with no expiry cannot be created.
    #[serde(rename = "expiresAt")]
    pub expires_at: chrono::DateTime<chrono::FixedOffset>,
    /// When it was last used. Absent if it never has been. Accurate to within a minute and deliberately no more: recording every request would put a database write behind every request, and this column answers \"is this still in use?\", which does not need the seconds. 
    #[serde(rename = "lastUsedAt", skip_serializing_if = "Option::is_none")]
    pub last_used_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    /// When somebody ended it early. Absent on a token nobody has revoked; expired and revoked are different facts and `status` says which. 
    #[serde(rename = "revokedAt", skip_serializing_if = "Option::is_none")]
    pub revoked_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    /// Which account ended it. Absent on a token nobody has revoked, and equal to the owner on one its owner rotated themselves — so a value here that is *not* the owner is an administrator having stepped in (`DELETE /users/{userId}/tokens/{tokenId}`), which is the question an incident review asks of the credential in front of it. 
    #[serde(rename = "revokedBy", skip_serializing_if = "Option::is_none")]
    pub revoked_by: Option<uuid::Uuid>,
}

impl ServiceToken {
    /// One service token, as anybody but its creator ever sees it: everything about the credential except the credential. 
    pub fn new(id: uuid::Uuid, name: String, prefix: String, scopes: Vec<models::TokenScope>, status: models::ServiceTokenStatus, created_at: chrono::DateTime<chrono::FixedOffset>, expires_at: chrono::DateTime<chrono::FixedOffset>) -> ServiceToken {
        ServiceToken {
            id,
            name,
            prefix,
            scopes,
            engagement_id: None,
            status,
            created_at,
            expires_at,
            last_used_at: None,
            revoked_at: None,
            revoked_by: None,
        }
    }
}

