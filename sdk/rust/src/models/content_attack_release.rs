/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ContentAttackRelease : One ATT&CK release, as something to choose between.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ContentAttackRelease {
    /// Upstream's own release label (for example `17.1`), and the string to send back as a sync pin. Not normalized: what upstream calls it is what a fetch will resolve. 
    #[serde(rename = "version")]
    pub version: String,
    /// When upstream published it. Absent when upstream does not say, and when the release is known only because it is installed here. 
    #[serde(rename = "released", skip_serializing_if = "Option::is_none")]
    pub released: Option<chrono::DateTime<chrono::FixedOffset>>,
    /// Whether this installation already holds a version row for this label, in any state. `status` says which. 
    #[serde(rename = "installed")]
    pub installed: bool,
    /// The installed version's state (`pending`, `syncing`, `ready`, `failed`). Absent when `installed` is false. A release that is installed but `failed` is still worth offering, which is why this is a status rather than a second boolean. 
    #[serde(rename = "status", skip_serializing_if = "Option::is_none")]
    pub status: Option<String>,
    /// Upstream's newest release. Exactly one item carries `true` when the index was read, and none do when it was not. 
    #[serde(rename = "latest")]
    pub latest: bool,
}

impl ContentAttackRelease {
    /// One ATT&CK release, as something to choose between.
    pub fn new(version: String, installed: bool, latest: bool) -> ContentAttackRelease {
        ContentAttackRelease {
            version,
            released: None,
            installed,
            status: None,
            latest,
        }
    }
}

