/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// CreatedServiceToken : A token that has just been created: the row, plus the one and only copy of its secret. Nothing else in this API ever carries `token`. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct CreatedServiceToken {
    #[serde(rename = "serviceToken")]
    pub service_token: models::ServiceToken,
    /// The credential, spelled `bl_<prefix>_<secret>`. Send it as `Authorization: Bearer <token>`.  Show it to the person immediately and offer a way to copy it. There is no endpoint that reads it back, because the server keeps only a hash of it — a lost token is replaced, never recovered. 
    #[serde(rename = "token")]
    pub token: String,
}

impl CreatedServiceToken {
    /// A token that has just been created: the row, plus the one and only copy of its secret. Nothing else in this API ever carries `token`. 
    pub fn new(service_token: models::ServiceToken, token: String) -> CreatedServiceToken {
        CreatedServiceToken {
            service_token,
            token,
        }
    }
}

