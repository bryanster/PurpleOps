/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ActivityEntry : One row of the append-only activity log (M1-015). Drives the SSE feed (M4) and the report timeline (M6). There is no update or delete. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ActivityEntry {
    /// UUIDv7. Sortable by creation; the stable tiebreaker when two rows share a timestamp.
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    /// The engagement this entry belongs to. Absent on platform events (logins, token lifecycle, role changes). 
    #[serde(rename = "engagementId", skip_serializing_if = "Option::is_none")]
    pub engagement_id: Option<uuid::Uuid>,
    /// Who did it. Absent when the actor is unknown — a failed login that named no account. 
    #[serde(rename = "actorId", skip_serializing_if = "Option::is_none")]
    pub actor_id: Option<uuid::Uuid>,
    /// What happened, spelled `object.past_tense_verb`. Examples: `session.login`, `session.login_failed`, `token.created`, `mfa.enrolled`. 
    #[serde(rename = "verb")]
    pub verb: String,
    /// The kind of thing acted on (`user`, `session`, `service_token`, …).
    #[serde(rename = "objectType")]
    pub object_type: String,
    /// The identifier of the thing acted on.
    #[serde(rename = "objectId")]
    pub object_id: String,
    /// Before/after for changed fields, already redacted. Never a password hash, token secret, TOTP secret, session token or recovery code. Absent when there is nothing useful to say beyond the verb. 
    #[serde(rename = "delta", skip_serializing_if = "Option::is_none")]
    pub delta: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// When it happened, UTC.
    #[serde(rename = "at")]
    pub at: chrono::DateTime<chrono::FixedOffset>,
    /// Whether the step this event is about has been revealed to blue. Present only for step-scoped objects (step, execution, evidence, comment) in a blind engagement. A blue caller's activity list omits rows where `revealed` is false (M4-008). 
    #[serde(rename = "revealed", skip_serializing_if = "Option::is_none")]
    pub revealed: Option<bool>,
}

impl ActivityEntry {
    /// One row of the append-only activity log (M1-015). Drives the SSE feed (M4) and the report timeline (M6). There is no update or delete. 
    pub fn new(id: uuid::Uuid, verb: String, object_type: String, object_id: String, at: chrono::DateTime<chrono::FixedOffset>) -> ActivityEntry {
        ActivityEntry {
            id,
            engagement_id: None,
            actor_id: None,
            verb,
            object_type,
            object_id,
            delta: None,
            at,
            revealed: None,
        }
    }
}

