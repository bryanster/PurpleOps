/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ChangePasswordRequest : Body of `POST /auth/password`. The current password is required even though the caller is signed in: a session left open on a shared machine must not be enough to take the account over. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ChangePasswordRequest {
    #[serde(rename = "currentPassword")]
    pub current_password: String,
    /// The replacement. The policy — at least 12 characters, at most 128, and not one of the passwords attackers try first — is applied by the server and reported as field errors on `newPassword`, so that there is one definition of an acceptable password rather than one here and one in the client. 
    #[serde(rename = "newPassword")]
    pub new_password: String,
}

impl ChangePasswordRequest {
    /// Body of `POST /auth/password`. The current password is required even though the caller is signed in: a session left open on a shared machine must not be enough to take the account over. 
    pub fn new(current_password: String, new_password: String) -> ChangePasswordRequest {
        ChangePasswordRequest {
            current_password,
            new_password,
        }
    }
}

