/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// UserPage : One page of accounts, oldest first — identifiers are UUIDv7, so that is creation order and a stable total order at the same time. `nextCursor` is taken verbatim from a previous response; absent (or null) means there is no further page. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct UserPage {
    #[serde(rename = "items")]
    pub items: Vec<models::User>,
    /// Opaque cursor for the next page. Pass it back as `cursor`.
    #[serde(rename = "nextCursor", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub next_cursor: Option<Option<String>>,
}

impl UserPage {
    /// One page of accounts, oldest first — identifiers are UUIDv7, so that is creation order and a stable total order at the same time. `nextCursor` is taken verbatim from a previous response; absent (or null) means there is no further page. 
    pub fn new(items: Vec<models::User>) -> UserPage {
        UserPage {
            items,
            next_cursor: None,
        }
    }
}

