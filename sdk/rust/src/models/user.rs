/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// User : One account, as user administration reports it. Compare `CurrentUser`, which is the caller describing themselves and carries their memberships and their MFA state; this is an administrator describing somebody else.  What is *not* here is the point of it: there is no password hash, no authenticator secret, no recovery code and no session token, so no response built from this schema can carry one. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct User {
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    /// The address as it was typed. Matched without regard to case everywhere it is looked up.
    #[serde(rename = "email")]
    pub email: String,
    #[serde(rename = "displayName")]
    pub display_name: String,
    #[serde(rename = "platformRole")]
    pub platform_role: models::PlatformRole,
    #[serde(rename = "status")]
    pub status: models::UserStatus,
    /// Whether an administrator requires a second factor of this person specifically. It is one input to the effective requirement and not the whole answer — see `MFAState.required`, which is what an interface acts on for the *signed-in* user. 
    #[serde(rename = "mfaEnforced")]
    pub mfa_enforced: bool,
    #[serde(rename = "createdAt")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "updatedAt")]
    pub updated_at: chrono::DateTime<chrono::FixedOffset>,
    /// When this account last signed in. Absent if it never has.
    #[serde(rename = "lastLoginAt", skip_serializing_if = "Option::is_none")]
    pub last_login_at: Option<chrono::DateTime<chrono::FixedOffset>>,
}

impl User {
    /// One account, as user administration reports it. Compare `CurrentUser`, which is the caller describing themselves and carries their memberships and their MFA state; this is an administrator describing somebody else.  What is *not* here is the point of it: there is no password hash, no authenticator secret, no recovery code and no session token, so no response built from this schema can carry one. 
    pub fn new(id: uuid::Uuid, email: String, display_name: String, platform_role: models::PlatformRole, status: models::UserStatus, mfa_enforced: bool, created_at: chrono::DateTime<chrono::FixedOffset>, updated_at: chrono::DateTime<chrono::FixedOffset>) -> User {
        User {
            id,
            email,
            display_name,
            platform_role,
            status,
            mfa_enforced,
            created_at,
            updated_at,
            last_login_at: None,
        }
    }
}

