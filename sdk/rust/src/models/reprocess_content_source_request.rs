/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ReprocessContentSourceRequest : Optional pin for which raw snapshot to reprocess. Additional properties are rejected so a mistyped field cannot silently no-op. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ReprocessContentSourceRequest {
    /// ATT&CK release label (e.g. `15.1`). Omit for rolling sources (`current`). Required for ATT&CK. 
    #[serde(rename = "version", skip_serializing_if = "Option::is_none")]
    pub version: Option<String>,
}

impl ReprocessContentSourceRequest {
    /// Optional pin for which raw snapshot to reprocess. Additional properties are rejected so a mistyped field cannot silently no-op. 
    pub fn new() -> ReprocessContentSourceRequest {
        ReprocessContentSourceRequest {
            version: None,
        }
    }
}

