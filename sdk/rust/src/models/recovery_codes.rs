/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// RecoveryCodes : A freshly minted set of recovery codes (M1-007), in the only response that ever carries them. Put them in front of the person immediately and offer a way to save or print them: there is no endpoint that reads them back, because the server stores only their hashes. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct RecoveryCodes {
    /// Ten single-use codes, each twenty characters of Crockford base32 — the digits and the uppercase letters, less `I`, `L`, `O` and `U`, so that no two characters in a code look alike. That is 100 bits each.  They arrive grouped in fours for transcription. The grouping is presentation: the server accepts them back in any case, with or without the hyphens, and folds the four omitted characters onto what they resemble (`O`→`0`, `I`/`L`→`1`) so that somebody's handwriting cannot lock them out. 
    #[serde(rename = "codes")]
    pub codes: Vec<String>,
    /// When this set was minted. It is what an interface shows next to \"you last replaced these on …\", and the moment every previous code stopped working. 
    #[serde(rename = "generatedAt")]
    pub generated_at: chrono::DateTime<chrono::FixedOffset>,
}

impl RecoveryCodes {
    /// A freshly minted set of recovery codes (M1-007), in the only response that ever carries them. Put them in front of the person immediately and offer a way to save or print them: there is no endpoint that reads them back, because the server stores only their hashes. 
    pub fn new(codes: Vec<String>, generated_at: chrono::DateTime<chrono::FixedOffset>) -> RecoveryCodes {
        RecoveryCodes {
            codes,
            generated_at,
        }
    }
}

