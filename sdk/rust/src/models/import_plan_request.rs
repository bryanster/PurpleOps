/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ImportPlanRequest : Request to import a CTID emulation plan as an engagement Scenario. At least one of `planId` or (`planExternalId` + `sourceId`) must be provided. If both are given `planId` takes precedence. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ImportPlanRequest {
    /// Content catalog surrogate id of the plan to import.
    #[serde(rename = "planId", skip_serializing_if = "Option::is_none")]
    pub plan_id: Option<uuid::Uuid>,
    /// External id of the plan (e.g. CTID upstream id or actor slug).
    #[serde(rename = "planExternalId", skip_serializing_if = "Option::is_none")]
    pub plan_external_id: Option<String>,
    /// Content source id. Required when `planExternalId` is used.
    #[serde(rename = "sourceId", skip_serializing_if = "Option::is_none")]
    pub source_id: Option<uuid::Uuid>,
    /// Override the scenario name (defaults to plan name).
    #[serde(rename = "name", skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Hint for the starting ordinal of the first imported step. Defaults to 1. Subsequent steps follow sequentially.
    #[serde(rename = "startingOrdinal", skip_serializing_if = "Option::is_none")]
    pub starting_ordinal: Option<i32>,
}

impl ImportPlanRequest {
    /// Request to import a CTID emulation plan as an engagement Scenario. At least one of `planId` or (`planExternalId` + `sourceId`) must be provided. If both are given `planId` takes precedence. 
    pub fn new() -> ImportPlanRequest {
        ImportPlanRequest {
            plan_id: None,
            plan_external_id: None,
            source_id: None,
            name: None,
            starting_ordinal: None,
        }
    }
}

