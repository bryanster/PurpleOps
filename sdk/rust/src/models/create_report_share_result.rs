/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct CreateReportShareResult {
    #[serde(rename = "share")]
    pub share: models::ReportShare,
    /// The share token. Shown once; never stored plaintext.
    #[serde(rename = "token", skip_serializing_if = "Option::is_none")]
    pub token: Option<String>,
    /// Absolute claim URL for sharing with recipients.
    #[serde(rename = "claimUrl")]
    pub claim_url: String,
}

impl CreateReportShareResult {
    pub fn new(share: models::ReportShare, claim_url: String) -> CreateReportShareResult {
        CreateReportShareResult {
            share,
            token: None,
            claim_url,
        }
    }
}

