/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ContentSourceKind : Closed vocabulary of content libraries. New kinds are a migration, not a string somebody passed to an API. There is no create-source endpoint in M2 — only the seeded rows. 
/// Closed vocabulary of content libraries. New kinds are a migration, not a string somebody passed to an API. There is no create-source endpoint in M2 — only the seeded rows. 
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum ContentSourceKind {
    #[serde(rename = "attack")]
    Attack,
    #[serde(rename = "atomic")]
    Atomic,
    #[serde(rename = "sigma")]
    Sigma,
    #[serde(rename = "ctid")]
    Ctid,
    #[serde(rename = "custom")]
    Custom,

}

impl std::fmt::Display for ContentSourceKind {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        match self {
            Self::Attack => write!(f, "attack"),
            Self::Atomic => write!(f, "atomic"),
            Self::Sigma => write!(f, "sigma"),
            Self::Ctid => write!(f, "ctid"),
            Self::Custom => write!(f, "custom"),
        }
    }
}

impl Default for ContentSourceKind {
    fn default() -> ContentSourceKind {
        Self::Attack
    }
}

