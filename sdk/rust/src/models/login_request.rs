/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// LoginRequest : Credentials for `POST /auth/login`.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct LoginRequest {
    /// The address the account was created with, matched without regard to case or surrounding whitespace. 
    #[serde(rename = "email")]
    pub email: String,
    /// The password, as typed. The bounds here are a backstop on an unauthenticated path, not the policy: the policy lives in one place (internal/authn/password) and applies where a password is *set*. Holding a login attempt to it would tell an attacker how long the password is not. 
    #[serde(rename = "password")]
    pub password: String,
}

impl LoginRequest {
    /// Credentials for `POST /auth/login`.
    pub fn new(email: String, password: String) -> LoginRequest {
        LoginRequest {
            email,
            password,
        }
    }
}

