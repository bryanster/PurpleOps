/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ContentNote : One freeform knowledge-base note under the custom source (or imported into it). Markdown body; optional technique link and tags. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ContentNote {
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    #[serde(rename = "sourceId")]
    pub source_id: uuid::Uuid,
    /// Version token. Custom is rolling-head and always `current`.
    #[serde(rename = "version")]
    pub version: String,
    /// Stable id within the custom source (defaults to the row id).
    #[serde(rename = "externalId")]
    pub external_id: String,
    #[serde(rename = "title")]
    pub title: String,
    /// Markdown body. Size-capped by server config.
    #[serde(rename = "bodyMarkdown")]
    pub body_markdown: String,
    #[serde(rename = "tags")]
    pub tags: Vec<String>,
    /// Optional ATT&CK technique external id (`T1059`, `T1059.001`). Empty string when unset. 
    #[serde(rename = "techniqueExternalId")]
    pub technique_external_id: String,
    #[serde(rename = "createdAt")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "updatedAt")]
    pub updated_at: chrono::DateTime<chrono::FixedOffset>,
}

impl ContentNote {
    /// One freeform knowledge-base note under the custom source (or imported into it). Markdown body; optional technique link and tags. 
    pub fn new(id: uuid::Uuid, source_id: uuid::Uuid, version: String, external_id: String, title: String, body_markdown: String, tags: Vec<String>, technique_external_id: String, created_at: chrono::DateTime<chrono::FixedOffset>, updated_at: chrono::DateTime<chrono::FixedOffset>) -> ContentNote {
        ContentNote {
            id,
            source_id,
            version,
            external_id,
            title,
            body_markdown,
            tags,
            technique_external_id,
            created_at,
            updated_at,
        }
    }
}

