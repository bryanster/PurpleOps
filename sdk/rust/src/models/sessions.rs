/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// Sessions : Your live sessions. Unpaginated, like `GET /auth/tokens` and for the same reason: this is one person's own credentials, the count is small and bounded by how many browsers they sign in from, and a page boundary in the middle of \"where am I signed in\" would be a list somebody could act on half of. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct Sessions {
    #[serde(rename = "items")]
    pub items: Vec<models::Session>,
}

impl Sessions {
    /// Your live sessions. Unpaginated, like `GET /auth/tokens` and for the same reason: this is one person's own credentials, the count is small and bounded by how many browsers they sign in from, and a page boundary in the middle of \"where am I signed in\" would be a list somebody could act on half of. 
    pub fn new(items: Vec<models::Session>) -> Sessions {
        Sessions {
            items,
        }
    }
}

