/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ContentDetectionRule : One detection rule reference (Sigma or custom). Reference only — Blacklight never executes, deploys, or converts rules to product queries (`PLAN.md` §3). 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ContentDetectionRule {
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    #[serde(rename = "sourceId")]
    pub source_id: uuid::Uuid,
    /// Version token. SigmaHQ is rolling-head and always `current`. 
    #[serde(rename = "version")]
    pub version: String,
    /// Stable id within the source. Prefer upstream rule `id` when present; otherwise the archive-relative path (documented in `docs/content-sigma.md`). 
    #[serde(rename = "externalId")]
    pub external_id: String,
    /// Rule title.
    #[serde(rename = "name")]
    pub name: String,
    #[serde(rename = "description")]
    pub description: String,
    /// ATT&CK technique external ids extracted from Sigma tags (`attack.t1059` → `T1059`). Only technique-mapped rules are stored. 
    #[serde(rename = "techniqueExternalIds")]
    pub technique_external_ids: Vec<String>,
    /// Sigma level (`informational`, `low`, `medium`, `high`, `critical`, …).
    #[serde(rename = "level")]
    pub level: String,
    /// Upstream rule status (`stable`, `test`, `experimental`, …).
    #[serde(rename = "status")]
    pub status: String,
    /// Upstream `logsource` object as JSON (category/product/service/…).
    #[serde(rename = "logsource")]
    pub logsource: std::collections::HashMap<String, serde_json::Value>,
    /// Full rule body as YAML text, sufficient to display or copy in the UI. Never executed by Blacklight. 
    #[serde(rename = "ruleYaml")]
    pub rule_yaml: String,
    #[serde(rename = "createdAt")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "updatedAt")]
    pub updated_at: chrono::DateTime<chrono::FixedOffset>,
}

impl ContentDetectionRule {
    /// One detection rule reference (Sigma or custom). Reference only — Blacklight never executes, deploys, or converts rules to product queries (`PLAN.md` §3). 
    pub fn new(id: uuid::Uuid, source_id: uuid::Uuid, version: String, external_id: String, name: String, description: String, technique_external_ids: Vec<String>, level: String, status: String, logsource: std::collections::HashMap<String, serde_json::Value>, rule_yaml: String, created_at: chrono::DateTime<chrono::FixedOffset>, updated_at: chrono::DateTime<chrono::FixedOffset>) -> ContentDetectionRule {
        ContentDetectionRule {
            id,
            source_id,
            version,
            external_id,
            name,
            description,
            technique_external_ids,
            level,
            status,
            logsource,
            rule_yaml,
            created_at,
            updated_at,
        }
    }
}

