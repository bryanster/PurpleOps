/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ContentDetectionLogsource : Structured logsource fields for a custom detection rule. Known keys only — request bodies must not accept free-form properties (PLAN.md §4). Upstream Sigma rules still store arbitrary logsource JSON on the read model. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ContentDetectionLogsource {
    #[serde(rename = "category", skip_serializing_if = "Option::is_none")]
    pub category: Option<String>,
    #[serde(rename = "product", skip_serializing_if = "Option::is_none")]
    pub product: Option<String>,
    #[serde(rename = "service", skip_serializing_if = "Option::is_none")]
    pub service: Option<String>,
    #[serde(rename = "definition", skip_serializing_if = "Option::is_none")]
    pub definition: Option<String>,
}

impl ContentDetectionLogsource {
    /// Structured logsource fields for a custom detection rule. Known keys only — request bodies must not accept free-form properties (PLAN.md §4). Upstream Sigma rules still store arbitrary logsource JSON on the read model. 
    pub fn new() -> ContentDetectionLogsource {
        ContentDetectionLogsource {
            category: None,
            product: None,
            service: None,
            definition: None,
        }
    }
}

