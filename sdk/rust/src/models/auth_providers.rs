/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// AuthProviders : What the login page may offer. It is deliberately a list rather than a set of booleans: SAML sits beside OIDC in it (M1-010), and a page that renders this array needed no change when it arrived. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct AuthProviders {
    /// Whether local email-and-password sign-in is offered. Always true today; it is stated rather than assumed so that a deployment which later turns it off has somewhere to say so. 
    #[serde(rename = "password")]
    pub password: bool,
    /// Every single sign-on provider that is configured **and** reachable right now. A configured provider that cannot be discovered is absent rather than listed-and-broken. 
    #[serde(rename = "sso")]
    pub sso: Vec<models::SsoProvider>,
}

impl AuthProviders {
    /// What the login page may offer. It is deliberately a list rather than a set of booleans: SAML sits beside OIDC in it (M1-010), and a page that renders this array needed no change when it arrived. 
    pub fn new(password: bool, sso: Vec<models::SsoProvider>) -> AuthProviders {
        AuthProviders {
            password,
            sso,
        }
    }
}

