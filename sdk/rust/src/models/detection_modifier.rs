/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// DetectionModifier : Descriptive flag qualifying a detection without changing its ordinal.
/// Descriptive flag qualifying a detection without changing its ordinal.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum DetectionModifier {
    #[serde(rename = "alert")]
    Alert,
    #[serde(rename = "correlated")]
    Correlated,
    #[serde(rename = "delayed")]
    Delayed,
    #[serde(rename = "config_change")]
    ConfigChange,
    #[serde(rename = "residual_artifact")]
    ResidualArtifact,

}

impl std::fmt::Display for DetectionModifier {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        match self {
            Self::Alert => write!(f, "alert"),
            Self::Correlated => write!(f, "correlated"),
            Self::Delayed => write!(f, "delayed"),
            Self::ConfigChange => write!(f, "config_change"),
            Self::ResidualArtifact => write!(f, "residual_artifact"),
        }
    }
}

impl Default for DetectionModifier {
    fn default() -> DetectionModifier {
        Self::Alert
    }
}

