/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// BlueDetectionPatch : Blue-side only PATCH body for an execution. `version` is the optimistic-lock field and is required on every call. Red fields are not present — red writes through a separate endpoint with its own type. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct BlueDetectionPatch {
    /// The version the caller read. Mismatch → 409.
    #[serde(rename = "version")]
    pub version: i32,
    #[serde(rename = "detectionCategory", skip_serializing_if = "Option::is_none")]
    pub detection_category: Option<models::DetectionCategory>,
    /// Qualifiers on the detection category. Multi-select; empty array allowed.
    #[serde(rename = "detectionModifiers", skip_serializing_if = "Option::is_none")]
    pub detection_modifiers: Option<Vec<models::DetectionModifier>>,
    #[serde(rename = "protection", skip_serializing_if = "Option::is_none")]
    pub protection: Option<models::Protection>,
    /// When the first detection fired (UTC).
    #[serde(rename = "detectedAt", skip_serializing_if = "Option::is_none")]
    pub detected_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    /// Source of detection (e.g. \"Splunk\", \"Sentinel\").
    #[serde(rename = "detectingSource", skip_serializing_if = "Option::is_none")]
    pub detecting_source: Option<String>,
    /// Reference to the detection rule that fired.
    #[serde(rename = "detectingRuleRef", skip_serializing_if = "Option::is_none")]
    pub detecting_rule_ref: Option<String>,
    #[serde(rename = "alertSeverity", skip_serializing_if = "Option::is_none")]
    pub alert_severity: Option<models::AlertSeverity>,
    /// Free-form notes from the blue operator.
    #[serde(rename = "blueNotes", skip_serializing_if = "Option::is_none")]
    pub blue_notes: Option<String>,
}

impl BlueDetectionPatch {
    /// Blue-side only PATCH body for an execution. `version` is the optimistic-lock field and is required on every call. Red fields are not present — red writes through a separate endpoint with its own type. 
    pub fn new(version: i32) -> BlueDetectionPatch {
        BlueDetectionPatch {
            version,
            detection_category: None,
            detection_modifiers: None,
            protection: None,
            detected_at: None,
            detecting_source: None,
            detecting_rule_ref: None,
            alert_severity: None,
            blue_notes: None,
        }
    }
}

