/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct Evidence {
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    /// SHA-256 hex digest of the blob content.
    #[serde(rename = "blobSha256")]
    pub blob_sha256: String,
    #[serde(rename = "filename")]
    pub filename: String,
    #[serde(rename = "caption")]
    pub caption: String,
    #[serde(rename = "side")]
    pub side: models::EvidenceSide,
    /// The execution this evidence is linked to.
    #[serde(rename = "executionId")]
    pub execution_id: uuid::Uuid,
    /// The comment this evidence is linked to (null when execution-only).
    #[serde(rename = "commentId", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub comment_id: Option<Option<uuid::Uuid>>,
    /// User id of the uploader.
    #[serde(rename = "uploadedBy")]
    pub uploaded_by: String,
    #[serde(rename = "uploadedAt")]
    pub uploaded_at: chrono::DateTime<chrono::FixedOffset>,
    /// File size in bytes.
    #[serde(rename = "size")]
    pub size: i32,
    /// Stored MIME type from upload.
    #[serde(rename = "mime")]
    pub mime: String,
}

impl Evidence {
    pub fn new(id: uuid::Uuid, blob_sha256: String, filename: String, caption: String, side: models::EvidenceSide, execution_id: uuid::Uuid, uploaded_by: String, uploaded_at: chrono::DateTime<chrono::FixedOffset>, size: i32, mime: String) -> Evidence {
        Evidence {
            id,
            blob_sha256,
            filename,
            caption,
            side,
            execution_id,
            comment_id: None,
            uploaded_by,
            uploaded_at,
            size,
            mime,
        }
    }
}

