/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ContentEmulationPlanStep : One ordered step under a CTID emulation plan. `ordinal` is 1-based document order from the upstream plan YAML (dense, unique per plan). Upstream `procedure_step` labels (for example `2.1`) live inside `procedure` when present and are not used as the ordinal. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ContentEmulationPlanStep {
    #[serde(rename = "id")]
    pub id: uuid::Uuid,
    #[serde(rename = "sourceId")]
    pub source_id: uuid::Uuid,
    /// Version token. CTID is rolling-head and always `current`.
    #[serde(rename = "version")]
    pub version: String,
    /// Surrogate id of the parent plan.
    #[serde(rename = "planId")]
    pub plan_id: uuid::Uuid,
    /// 1-based position under the plan (document order).
    #[serde(rename = "ordinal")]
    pub ordinal: i32,
    /// Stable id within the source. Prefer upstream step `id`; otherwise `{plan_external_id}/{ordinal}` (documented in `docs/content-ctid.md`). 
    #[serde(rename = "externalId")]
    pub external_id: String,
    #[serde(rename = "name")]
    pub name: String,
    #[serde(rename = "description")]
    pub description: String,
    /// ATT&CK technique external id when upstream provides one (for example `T1566.001`). Empty string when missing — allowed; M3 pin resolve treats empty as no technique. 
    #[serde(rename = "techniqueExternalId")]
    pub technique_external_id: String,
    /// Structured procedure-ish payload when upstream provides commands: platforms, executors (name/command/cleanup), input_arguments, tactic, procedure_group/step labels, cti_source, dependencies. Empty object when none. Snapshot onto scenario steps in M3-012 — never executed by Blacklight. 
    #[serde(rename = "procedure")]
    pub procedure: std::collections::HashMap<String, serde_json::Value>,
    #[serde(rename = "createdAt")]
    pub created_at: chrono::DateTime<chrono::FixedOffset>,
    #[serde(rename = "updatedAt")]
    pub updated_at: chrono::DateTime<chrono::FixedOffset>,
}

impl ContentEmulationPlanStep {
    /// One ordered step under a CTID emulation plan. `ordinal` is 1-based document order from the upstream plan YAML (dense, unique per plan). Upstream `procedure_step` labels (for example `2.1`) live inside `procedure` when present and are not used as the ordinal. 
    pub fn new(id: uuid::Uuid, source_id: uuid::Uuid, version: String, plan_id: uuid::Uuid, ordinal: i32, external_id: String, name: String, description: String, technique_external_id: String, procedure: std::collections::HashMap<String, serde_json::Value>, created_at: chrono::DateTime<chrono::FixedOffset>, updated_at: chrono::DateTime<chrono::FixedOffset>) -> ContentEmulationPlanStep {
        ContentEmulationPlanStep {
            id,
            source_id,
            version,
            plan_id,
            ordinal,
            external_id,
            name,
            description,
            technique_external_id,
            procedure,
            created_at,
            updated_at,
        }
    }
}

