/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ContentAttackVersion : One installed ATT&CK release as the pin surface exposes it. The `version` string is what engagements will store as `attack_version` (M3) — opaque equality to `content_source_version.version`. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ContentAttackVersion {
    /// Release label (for example `15.1`).
    #[serde(rename = "version")]
    pub version: String,
    #[serde(rename = "status")]
    pub status: models::ContentSourceVersionStatus,
    #[serde(rename = "itemCount")]
    pub item_count: i64,
    /// When this version last finished successfully. Absent if never.
    #[serde(rename = "syncedAt", skip_serializing_if = "Option::is_none")]
    pub synced_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    /// Whether the ATT&CK source is currently enabled. A disabled source cannot accept new pins even when this version row is ready. 
    #[serde(rename = "sourceEnabled")]
    pub source_enabled: bool,
}

impl ContentAttackVersion {
    /// One installed ATT&CK release as the pin surface exposes it. The `version` string is what engagements will store as `attack_version` (M3) — opaque equality to `content_source_version.version`. 
    pub fn new(version: String, status: models::ContentSourceVersionStatus, item_count: i64, source_enabled: bool) -> ContentAttackVersion {
        ContentAttackVersion {
            version,
            status,
            item_count,
            synced_at: None,
            source_enabled,
        }
    }
}

