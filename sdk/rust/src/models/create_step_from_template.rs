/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct CreateStepFromTemplate {
    /// Content catalog surrogate id of the procedure template.
    #[serde(rename = "templateId")]
    pub template_id: uuid::Uuid,
    /// Override the step name (defaults to the template name).
    #[serde(rename = "name", skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    /// Override the step objective (defaults to template description).
    #[serde(rename = "objective", skip_serializing_if = "Option::is_none")]
    pub objective: Option<String>,
    /// Target asset for this step.
    #[serde(rename = "targetAsset", skip_serializing_if = "Option::is_none")]
    pub target_asset: Option<String>,
    /// Map of template input arg name to value. `#{key}` placeholders in command and cleanup are replaced with the corresponding value. Keys without a provided value are left as-is in the snapshot. 
    #[serde(rename = "argValues", skip_serializing_if = "Option::is_none")]
    pub arg_values: Option<std::collections::HashMap<String, String>>,
}

impl CreateStepFromTemplate {
    pub fn new(template_id: uuid::Uuid) -> CreateStepFromTemplate {
        CreateStepFromTemplate {
            template_id,
            name: None,
            objective: None,
            target_asset: None,
            arg_values: None,
        }
    }
}

