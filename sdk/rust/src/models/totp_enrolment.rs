/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// TotpEnrolment : A newly minted, unconfirmed authenticator secret, in the three forms a person might need to get it into an app. Every field carries the same secret; this is the only response in the API that does. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct TotpEnrolment {
    /// The `otpauth://totp/...` URI, for a client that can hand a URI to an authenticator directly. 
    #[serde(rename = "otpauthUri")]
    pub otpauth_uri: String,
    /// The base32 shared secret, for typing in by hand. It is the same value that is inside `otpauthUri`, spelled out so a client does not have to parse one to show the other. 
    #[serde(rename = "secret")]
    pub secret: String,
    /// The URI rendered as a PNG in a `data:` URI, ready to be the `src` of an `<img>`. A data URI rather than SVG markup so that showing it does not mean putting server-supplied markup into the page; the content security policy already allows `img-src data:`. 
    #[serde(rename = "qrCode")]
    pub qr_code: String,
}

impl TotpEnrolment {
    /// A newly minted, unconfirmed authenticator secret, in the three forms a person might need to get it into an app. Every field carries the same secret; this is the only response in the API that does. 
    pub fn new(otpauth_uri: String, secret: String, qr_code: String) -> TotpEnrolment {
        TotpEnrolment {
            otpauth_uri,
            secret,
            qr_code,
        }
    }
}

