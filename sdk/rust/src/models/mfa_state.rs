/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// MfaState : Where this person and this session stand on multi-factor authentication. Whether an administrator *requires* it and whether this session has *satisfied* it are separate facts — conflating them is the hole M1-008 closes. 
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct MfaState {
    /// An administrator requires a second factor of *this person specifically* — the per-user flag, set through the user administration API.  It is one of the inputs to `required`, and not the whole answer: somebody can be required to hold a factor by the platform policy with this flag off. A client deciding whether to block should read `required`. 
    #[serde(rename = "enforced")]
    pub enforced: bool,
    /// Whether this person must hold a second factor: `enforced`, or the platform policy (`GET /settings/mfa`) applying to them. This is the effective answer and the one an interface acts on (M1-008).  `required` with `enrolled` false is the state that confines a session to enrolling: every other endpoint answers `403` with `code` `mfa_enrolment_required` until an enrolment is confirmed.  An account that signs in only through an identity provider is exempt, and reports `false` — the provider owns its factors. See `docs/security.md`. 
    #[serde(rename = "required")]
    pub required: bool,
    /// This person has a confirmed authenticator (M1-006). A *started* but unconfirmed enrolment does not count — it gates nothing, so reporting it as enrolled would be a lie the interface acted on.  Separate from `enforced` for the same reason `satisfied` is: \"required to\" and \"has\" are different facts, and treating one as evidence of the other is the hole M1-008 closes. 
    #[serde(rename = "enrolled")]
    pub enrolled: bool,
    /// A second factor was presented for *this* session.
    #[serde(rename = "satisfied")]
    pub satisfied: bool,
    /// How many unused recovery codes this person holds (M1-007). `0` for somebody who has not enrolled: codes are minted when an enrolment is confirmed and deleted when it is removed.  A count and never the codes — those were shown once, and the server keeps only their hashes. Warn below three: the number matters most to the person who is one lost phone away from having no way in. 
    #[serde(rename = "recoveryCodesRemaining")]
    pub recovery_codes_remaining: i32,
}

impl MfaState {
    /// Where this person and this session stand on multi-factor authentication. Whether an administrator *requires* it and whether this session has *satisfied* it are separate facts — conflating them is the hole M1-008 closes. 
    pub fn new(enforced: bool, required: bool, enrolled: bool, satisfied: bool, recovery_codes_remaining: i32) -> MfaState {
        MfaState {
            enforced,
            required,
            enrolled,
            satisfied,
            recovery_codes_remaining,
        }
    }
}

