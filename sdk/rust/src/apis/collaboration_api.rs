/*
 * Blacklight API
 *
 * The HTTP API of a Blacklight deployment. Single-tenant, single-node: this document describes the one server the SPA talks to, and the same endpoints are the supported integration surface for scripts and CI.  All timestamps are UTC, RFC 3339. All identifiers are UUIDv7 strings — sortable by creation time, so `ORDER BY id` is a stable tiebreaker. Errors are RFC 9457 problem documents; see the `Problem` schema. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 * Generated by: https://openapi-generator.tech
 */


use reqwest;
use serde::{Deserialize, Serialize, de::Error as _};
use crate::{apis::ResponseContent, models};
use super::{Error, configuration, ContentType};


/// struct for typed errors of method [`create_comment`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CreateCommentError {
    Status400(models::Problem),
    Status401(models::Problem),
    Status403(models::Problem),
    Status404(models::Problem),
    Status409(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`delete_engagement_presence`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum DeleteEngagementPresenceError {
    Status400(models::Problem),
    Status401(models::Problem),
    Status403(models::Problem),
    Status404(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_engagement_presence`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetEngagementPresenceError {
    Status401(models::Problem),
    Status403(models::Problem),
    Status404(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`list_comment_revisions`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum ListCommentRevisionsError {
    Status401(models::Problem),
    Status403(models::Problem),
    Status404(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`list_comments`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum ListCommentsError {
    Status401(models::Problem),
    Status403(models::Problem),
    Status404(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`patch_comment`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PatchCommentError {
    Status401(models::Problem),
    Status403(models::Problem),
    Status404(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`put_engagement_presence`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PutEngagementPresenceError {
    Status400(models::Problem),
    Status401(models::Problem),
    Status403(models::Problem),
    Status404(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`subscribe_events`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum SubscribeEventsError {
    Status400(models::Problem),
    Status401(models::Problem),
    Status403(models::Problem),
    Status500(models::Problem),
    UnknownValue(serde_json::Value),
}


/// Write one comment. The author is the authenticated caller. Observers hold `comment.write` — it is their only write. Cannot comment on an unrevealed execution in blind mode. Closed engagements still accept comments. 
pub async fn create_comment(configuration: &configuration::Configuration, engagement_id: &str, execution_id: &str, create_comment: models::CreateComment, x_csrf_token: Option<&str>) -> Result<models::Comment, Error<CreateCommentError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_engagement_id = engagement_id;
    let p_path_execution_id = execution_id;
    let p_body_create_comment = create_comment;
    let p_header_x_csrf_token = x_csrf_token;

    let uri_str = format!("{}/engagements/{engagementId}/executions/{executionId}/comments", configuration.base_path, engagementId=crate::apis::urlencode(p_path_engagement_id), executionId=crate::apis::urlencode(p_path_execution_id));
    let mut req_builder = configuration.client.request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(param_value) = p_header_x_csrf_token {
        req_builder = req_builder.header("X-CSRF-Token", param_value.to_string());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };
    req_builder = req_builder.json(&p_body_create_comment);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::Comment`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::Comment`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<CreateCommentError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Explicitly leave presence for the authenticated session. The server publishes a `presence.leave` event on `engagement.{engagementId}`. A missing `presenceId` removes every entry for this user in this engagement (last tab closed).  TTL eviction is the source of truth; this is best-effort cleanup. Clients SHOULD call this on tab close via `navigator.sendBeacon`. 
pub async fn delete_engagement_presence(configuration: &configuration::Configuration, engagement_id: &str, x_csrf_token: Option<&str>, presence_id: Option<&str>) -> Result<(), Error<DeleteEngagementPresenceError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_engagement_id = engagement_id;
    let p_header_x_csrf_token = x_csrf_token;
    let p_query_presence_id = presence_id;

    let uri_str = format!("{}/engagements/{engagementId}/presence", configuration.base_path, engagementId=crate::apis::urlencode(p_path_engagement_id));
    let mut req_builder = configuration.client.request(reqwest::Method::DELETE, &uri_str);

    if let Some(ref param_value) = p_query_presence_id {
        req_builder = req_builder.query(&[("presenceId", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(param_value) = p_header_x_csrf_token {
        req_builder = req_builder.header("X-CSRF-Token", param_value.to_string());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<DeleteEngagementPresenceError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Returns a snapshot of every user whose presence heartbeat has not expired. The online user list is visible to all members; focus targets (`stepId` / `executionId`) are stripped for blue viewers when the referenced step is unrevealed under blind mode. 
pub async fn get_engagement_presence(configuration: &configuration::Configuration, engagement_id: &str) -> Result<models::GetEngagementPresence200Response, Error<GetEngagementPresenceError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_engagement_id = engagement_id;

    let uri_str = format!("{}/engagements/{engagementId}/presence", configuration.base_path, engagementId=crate::apis::urlencode(p_path_engagement_id));
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::GetEngagementPresence200Response`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::GetEngagementPresence200Response`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<GetEngagementPresenceError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Returns every prior body of this comment, oldest first. The current body is not included — it is on the comment itself. An edit appends the previous body, so the list is complete from creation to the penultimate edit. 
pub async fn list_comment_revisions(configuration: &configuration::Configuration, engagement_id: &str, comment_id: &str) -> Result<Vec<models::CommentRevision>, Error<ListCommentRevisionsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_engagement_id = engagement_id;
    let p_path_comment_id = comment_id;

    let uri_str = format!("{}/engagements/{engagementId}/comments/{commentId}/revisions", configuration.base_path, engagementId=crate::apis::urlencode(p_path_engagement_id), commentId=crate::apis::urlencode(p_path_comment_id));
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `Vec&lt;models::CommentRevision&gt;`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `Vec&lt;models::CommentRevision&gt;`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<ListCommentRevisionsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Returns every comment on this execution, oldest first. Members of the engagement may read comments. In blind mode, comments on an unrevealed step are 404-concealed. 
pub async fn list_comments(configuration: &configuration::Configuration, engagement_id: &str, execution_id: &str) -> Result<Vec<models::Comment>, Error<ListCommentsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_engagement_id = engagement_id;
    let p_path_execution_id = execution_id;

    let uri_str = format!("{}/engagements/{engagementId}/executions/{executionId}/comments", configuration.base_path, engagementId=crate::apis::urlencode(p_path_engagement_id), executionId=crate::apis::urlencode(p_path_execution_id));
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `Vec&lt;models::Comment&gt;`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `Vec&lt;models::Comment&gt;`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<ListCommentsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Only the comment's author, the engagement lead, or a platform administrator may edit. The previous body is appended to the revision history. No delete — history is append-only. 
pub async fn patch_comment(configuration: &configuration::Configuration, engagement_id: &str, comment_id: &str, patch_comment: models::PatchComment, x_csrf_token: Option<&str>) -> Result<models::Comment, Error<PatchCommentError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_engagement_id = engagement_id;
    let p_path_comment_id = comment_id;
    let p_body_patch_comment = patch_comment;
    let p_header_x_csrf_token = x_csrf_token;

    let uri_str = format!("{}/engagements/{engagementId}/comments/{commentId}", configuration.base_path, engagementId=crate::apis::urlencode(p_path_engagement_id), commentId=crate::apis::urlencode(p_path_comment_id));
    let mut req_builder = configuration.client.request(reqwest::Method::PATCH, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(param_value) = p_header_x_csrf_token {
        req_builder = req_builder.header("X-CSRF-Token", param_value.to_string());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };
    req_builder = req_builder.json(&p_body_patch_comment);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::Comment`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::Comment`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<PatchCommentError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Upsert a presence heartbeat for the authenticated session. The caller provides a client-generated `presenceId` so multiple tabs in the same browser are distinguished. An optional `focus` object reports which step or execution the user is currently viewing.  The server publishes ephemeral `presence.join` and `presence.update` events on `engagement.{engagementId}`. These events use hub-generated ids and are not replayed on `Last-Event-ID` reconnect. 
pub async fn put_engagement_presence(configuration: &configuration::Configuration, engagement_id: &str, presence_heartbeat: models::PresenceHeartbeat, x_csrf_token: Option<&str>) -> Result<(), Error<PutEngagementPresenceError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_path_engagement_id = engagement_id;
    let p_body_presence_heartbeat = presence_heartbeat;
    let p_header_x_csrf_token = x_csrf_token;

    let uri_str = format!("{}/engagements/{engagementId}/presence", configuration.base_path, engagementId=crate::apis::urlencode(p_path_engagement_id));
    let mut req_builder = configuration.client.request(reqwest::Method::PUT, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(param_value) = p_header_x_csrf_token {
        req_builder = req_builder.header("X-CSRF-Token", param_value.to_string());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };
    req_builder = req_builder.json(&p_body_presence_heartbeat);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();

    if !status.is_client_error() && !status.is_server_error() {
        Ok(())
    } else {
        let content = resp.text().await?;
        let entity: Option<PutEngagementPresenceError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

/// Opens a long-lived `text/event-stream` for live UI updates. M2 streams content sync job progress; M4 extends the same hub with engagement topics.  **Session cookie only.** Service tokens are refused — EventSource cannot attach an Authorization header, and a long-lived token-authenticated stream is not a browser subscription. The operation's `security` lists only `cookieSession`, and the authorization middleware refuses `MethodServiceToken`.  Query `topics` is repeatable. Per-topic authorization is enforced in the SSE handler (not in the middleware — this endpoint maps to a self-service operation so any authenticated session may subscribe, then TopicAuthz filters per topic). Unknown topic names are `400` (never silently widened). Topics:  - `content.jobs` / `content.jobs.{jobId}` — requires   `content.sync` (administrators) - `engagement.{engagementId}` — requires `engagement.read`   (members and administrators of that engagement)  Event `type` values are stable: `content.job.progress` while a job runs, `content.job.terminal` once when it ends. Each frame carries `id` (UUIDv7), `event` (the type), and a JSON `data` payload.  `Last-Event-ID` is accepted but **best-effort only in M2**: there is no replay against the activity log yet. On reconnect, the SPA should reconcile from `GET /content/jobs/{id}`. M4 owns guaranteed catch-up.  Heartbeat comment frames (`: ping`) are written periodically so idle reverse proxies do not close the connection. Operators must disable response buffering on this path — see `docs/deploy.md`. 
pub async fn subscribe_events(configuration: &configuration::Configuration, topics: Option<Vec<String>>, last_event_id: Option<&str>, last_event_id2: Option<&str>) -> Result<String, Error<SubscribeEventsError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_query_topics = topics;
    let p_header_last_event_id = last_event_id;
    let p_query_last_event_id = last_event_id2;

    let uri_str = format!("{}/events", configuration.base_path);
    let mut req_builder = configuration.client.request(reqwest::Method::GET, &uri_str);

    if let Some(ref param_value) = p_query_topics {
        req_builder = match "multi" {
            "multi" => req_builder.query(&param_value.into_iter().map(|p| ("topics".to_owned(), p.to_string())).collect::<Vec<(std::string::String, std::string::String)>>()),
            _ => req_builder.query(&[("topics", &param_value.into_iter().map(|p| p.to_string()).collect::<Vec<String>>().join(",").to_string())]),
        };
    }
    if let Some(ref param_value) = p_query_last_event_id {
        req_builder = req_builder.query(&[("lastEventId", &param_value.to_string())]);
    }
    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(param_value) = p_header_last_event_id {
        req_builder = req_builder.header("Last-Event-ID", param_value.to_string());
    }

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `String`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `String`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<SubscribeEventsError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent { status, content, entity }))
    }
}

