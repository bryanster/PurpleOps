# mini-plan.yaml — fixture CTID plan (≥3 steps, technique links)
- emulation_plan_details:
    id: 11111111-1111-1111-1111-111111111111
    adversary_name: Fixture Eagle
    adversary_description: A small fixture adversary for CI.
    attack_version: "14.1"
    format_version: "1.0"

- id: step-aaaa-0001
  name: Initial foothold
  description: Establish initial access via phishing attachment.
  tactic: initial-access
  technique:
    attack_id: T1566.001
    name: "Phishing: Spearphishing Attachment"
  cti_source: https://example.test/fixture-eagle
  procedure_group: procedure_initial_access
  procedure_step: "1.1"
  platforms:
    windows:
      cmd:
        command: |
          echo foothold
  executors:
  - name: command_prompt
    command: |
      echo foothold
  input_arguments:
    target_user:
      description: User mailbox to target
      type: string
      default: user@example.test

- id: step-bbbb-0002
  name: Enumerate domain users
  description: List domain accounts with a discovery tool.
  tactic: discovery
  technique:
    attack_id: T1087.002
    name: "Account Discovery: Domain Account"
  procedure_group: procedure_discovery
  procedure_step: "2.1"
  platforms:
    windows:
      cmd:
        command: |
          net user /domain
  executors:
  - name: command_prompt
    command: |
      net user /domain
    cleanup: |
      echo cleaned

- id: step-cccc-0003
  name: Manual note without technique
  description: Operator observation step with no ATT&CK mapping.
  tactic: collection
  procedure_group: procedure_collection
  procedure_step: "3.1"
  platforms:
    windows:
      manual:
        command: |
          Review collected files offline.
  executors:
  - name: manual
    command: |
      Review collected files offline.
