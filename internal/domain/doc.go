// Package domain holds the entities and the rules that operate on them:
// engagements, scenarios, steps and retest rounds, plus ATT&CK Evaluations
// scoring, mean-time-to-detect, rollups and retest deltas.
//
// It depends on no transport and no database package, so the rules can be
// tested as pure functions.
//
// Implemented by M3.
package domain
